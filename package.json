{
  "name": "reason-store",
  "version": "0.1.0",
  "scripts": {
    "build": "bsb -make-world",
    "start": "bsb -make-world -w -ws _ ",
    "clean": "bsb -clean-world",
    "server": "moduleserve ./demo --port 8000",
    "test": "echo \"Error: no test specified\" && exit 1",
    "example": "moduleserve ./example --port 8000"
  },
  "keywords": [
    "BuckleScript",
    "ReasonReact",
    "reason-react"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "store": "^2.0.12"
  },
  "devDependencies": {
    "bs-platform": "^8.3.3",
    "moduleserve": "^0.9.0",
    "tape": "^5.0.1",
    "tinytest": "^1.1.5"
  },
  "peerDependencies": {
    "store": "^2.0.12",
    "bs-platform": "*"
  }
}
